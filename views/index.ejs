<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Whatsapp</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h2>All Chats !</h2>
    <form action="/chat/new" method="get"> 
        <button>New Chat</button>
    </form>
    <br>
    
    <div class="box">
        <% for( let chat of chats ){ %>
            <div class="chat">
                <p>From:<%=chat.from%></p>
                <div class="msg">
                    <p><%=chat.msg%></p>
                </div>
               
                <p>Recived By:<%=chat.to%></p>
                <hr>
                <p><%=chat.created_at.toString().split(" ")[4]%>  </p>
                <p><%=chat.created_at.toString().split(" ").slice(0 ,4).join("-")%></p>

                <div class="btn">
                    <form action="/chat/<%=chat._id%>/edit" method="get">
                        <button class="edit">Edit</button>
                    </form>
                    <form action="/chat/<%=chat._id%>/delete" method="get">
                        <button id="dlt" >Delete</button>
                    </form>
                    <form action="/chat/<%=chat._id%>" method="GET">
                        <button id="dlt" >show</button>
                    </form>
                </div>
               
            </div>

            <%}%>
    </div>
   

</body>
</html>